namespace CoopQueue.Shared.Enums
{
    public enum UserRole
    {
        User = 0,   // Standard access (Can vote, suggest games)
        Admin = 1   // Full access (Can delete games, manage users)
    }
}